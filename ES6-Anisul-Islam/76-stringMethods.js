// all these methods are case sensitive and ans in boolean value

// starWith (searchString, position) ==> check a string starts with another string

const message = 'Today is friday'
console.log(message.startsWith('Today'))

// endsWith (searchString, length) ==> check a string ends with another string

const msg = 'Today is Thursday'
console.log(msg.endsWith('Thursday'))

// includes (searchString) ==> check if a string contains another string

const msgIncludes = 'Today is Thursday'
console.log(msg.includes('thursday'))